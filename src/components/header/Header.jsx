import header from './Header.module.css'
export const Header = () => {
    return (
        <header className={header.header}>
            <span>Логотип</span>
        </header>
    )
}
export default Header;